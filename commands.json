{
 "$comment": "The machine-readable command manifest of ks. One row per verb the binary dispatches; manifest_test.go fails the build if this file and the source disagree in either direction. Downstream (keepstate.ai/docs/cli and its verb-count lint) renders FROM this file and must never hand-type a verb. Rows may carry `safety` (a refusal the product makes on the customer's behalf, which the docs page renders as the safety property it is) and `budgetDefaults` (the per-tier session token budget). Both are DESCRIPTIONS OF MEASURED OR SOURCED BEHAVIOUR, and manifest_test.go fails if a row states one without saying where the number came from. A two-word verb (`cruise init`) is a subcommand: the first word is the case arm in main.go, the second the case arm in cruise.go's runCruise; both directions are checked. Rows may carry `flags`, one entry per flag with its one-line meaning. `globalFlags` lists the flags every verb accepts after its own options, exactly as output.go defines them; manifest_test.go fails if the two differ in either direction.",
 "schema": 1,
 "globalFlags": [
  {
   "flag": "--json",
   "summary": "stable JSON on stdout: schema_version, request_id, data or error; progress stays on stderr"
  },
  {
   "flag": "--plain",
   "summary": "one fact per line, no alignment or colour (the human output is already plain; this pins it)"
  },
  {
   "flag": "--no-color",
   "summary": "no colour codes (this release prints none; accepted so scripts can pass it)"
  },
  {
   "flag": "--quiet",
   "summary": "no progress lines on stderr; results and errors still print"
  },
  {
   "flag": "--no-input",
   "summary": "never prompt or open a browser; a decision that needs you fails with exit 2 and the flag to pass"
  },
  {
   "flag": "--yes",
   "summary": "skip a displayed non-destructive confirmation when every input is already exact; never a deletion plan, grant, budget, approval or trust"
  },
  {
   "flag": "--wait-timeout DURATION",
   "summary": "how long to wait for an accepted operation before reporting it as continuing; default 120s"
  }
 ],
 "commands": [
  {
   "verb": "login",
   "aliases": [],
   "usage": "ks login [--ctl URL]",
   "summary": "sign in (browser device flow)",
   "status": "available",
   "surface": "client",
   "flags": [
    {
     "flag": "--ctl URL",
     "summary": "the control plane to sign in to; default https://ctl.keepstate.ai"
    }
   ]
  },
  {
   "verb": "run",
   "aliases": [],
   "usage": "ks run [--image I] [--budget-tokens N] [--agent] [--name NAME] [--agent-name NAME] [--task TEXT] [--open]",
   "summary": "start a hosted session; with --agent, an agent session whose main agent is Ready",
   "status": "available",
   "surface": "hosted",
   "budgetDefaults": {
    "unit": "tokens",
    "free": 500000,
    "paid": 2000000,
    "freeProvenance": "measured 2026-09-09 on the released client against a disposable free-tier account (DEC-01 command gate)",
    "paidProvenance": "stated by the control plane, ctl/broker.go, and the ratified price book v1.2. NOT measured: no paid account was exercised.",
    "override": "ks run --budget-tokens N"
   },
   "flags": [
    {
     "flag": "--image I",
     "summary": "the image of a plain session; default your onboarding choice (not with --agent)"
    },
    {
     "flag": "--budget-tokens N (also --budget)",
     "summary": "the session's token budget; default per tier (500,000 free, 2,000,000 paid); zero and negative are refused"
    },
    {
     "flag": "--agent",
     "summary": "start an agent session: a session, its primary agent and a machine, Ready only when the agent says so"
    },
    {
     "flag": "--name NAME",
     "summary": "the agent session's name (with --agent); default run-<UTC time>"
    },
    {
     "flag": "--agent-name NAME",
     "summary": "the primary agent's name (with --agent); default main"
    },
    {
     "flag": "--task TEXT",
     "summary": "a first task for the agent, submitted after Ready (with --agent)"
    },
    {
     "flag": "--open",
     "summary": "open the agent's window after Ready (with --agent)"
    }
   ]
  },
  {
   "verb": "checkpoint",
   "aliases": [
    "save"
   ],
   "usage": "ks checkpoint <session> [--stop]",
   "summary": "save the session (durably); --stop parks it",
   "status": "available",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--stop",
     "summary": "park the session after the save succeeds"
    }
   ]
  },
  {
   "verb": "wake",
   "aliases": [
    "resume"
   ],
   "usage": "ks wake <session>",
   "summary": "resume a parked session (mid-sentence)",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "kill",
   "aliases": [],
   "usage": "ks kill <session> [--force]",
   "summary": "destroy a session (guarded: checkpoint first)",
   "status": "available",
   "surface": "hosted",
   "safety": "Refuses a session that has no saved checkpoint, because killing it would destroy unsaved work, and names both ways out: `ks checkpoint <id> --stop` saves and stops it, `ks kill <id> --force` destroys it anyway.",
   "safetyMeasured": "2026-09-09, DEC-01 command gate, released client against a disposable account. The guard is enforced by the daemon, not the client, so it holds for every caller including the console.",
   "flags": [
    {
     "flag": "--force",
     "summary": "destroy it even without a saved checkpoint"
    }
   ]
  },
  {
   "verb": "meter",
   "aliases": [],
   "usage": "ks meter <session>",
   "summary": "spend, budget, key sources",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "exec",
   "aliases": [],
   "usage": "ks exec [--shell] <session> [--] <command...>",
   "summary": "run one command in the session",
   "status": "available",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--shell",
     "summary": "send the one argument verbatim for the session's shell to interpret (pipes, globs, variables); without it every argument reaches the program exactly as typed"
    }
   ]
  },
  {
   "verb": "attach",
   "aliases": [],
   "usage": "ks attach <session>",
   "summary": "interactive terminal into a running session (tmux)",
   "status": "available",
   "surface": "hosted",
   "note": "relays the guest tmux over a control-plane upgrade tunnel; live resize is a v1 limitation"
  },
  {
   "verb": "fork",
   "aliases": [],
   "usage": "ks fork <session> [-n N] [--steer FILE]",
   "summary": "branch a checkpoint into diverging children",
   "status": "available",
   "surface": "hosted",
   "note": "paid plans only; each child is another running session",
   "flags": [
    {
     "flag": "-n N",
     "summary": "how many children to branch; default 1"
    },
    {
     "flag": "--steer FILE",
     "summary": "a steer file applied to every child"
    }
   ]
  },
  {
   "verb": "cruise init",
   "aliases": [],
   "usage": "ks cruise init [--allow PATH...] [--goal TEXT] [--tests CMD] [--paths GLOB...] [--ladder family:model,...] [--spend USD]",
   "summary": "draft the acceptance manifest from the repository into .keepstate/cruise.json",
   "status": "available",
   "surface": "client",
   "flags": [
    {
     "flag": "--allow PATH...",
     "summary": "review a sensitive or ignored path into the upload (repeatable); see ks cruise preview"
    },
    {
     "flag": "--goal TEXT",
     "summary": "the goal the agent is given; without it, the draft's previous goal, else \"make the check pass: CMD\""
    },
    {
     "flag": "--tests CMD",
     "summary": "the command that decides done, when init should not detect it (pytest, npm test, go test)"
    },
    {
     "flag": "--paths GLOB...",
     "summary": "the paths an attempt may change; default every tracked file except the tests and .keepstate"
    },
    {
     "flag": "--ladder family:model,...",
     "summary": "the rungs in order; default the model table's default ladder"
    },
    {
     "flag": "--spend USD",
     "summary": "the spend ceiling in dollars; default 2"
    }
   ],
   "note": "makes no request and never calls a model; a repository with no check is refused with exit 2 and nothing written"
  },
  {
   "verb": "cruise approve",
   "aliases": [],
   "usage": "ks cruise approve",
   "summary": "lock the draft: its digest and the time into .keepstate/cruise.lock",
   "status": "available",
   "surface": "client",
   "note": "makes no request; refused when the workspace changed since init"
  },
  {
   "verb": "cruise run",
   "aliases": [],
   "usage": "ks cruise run",
   "summary": "upload the workspace and the locked manifest; start the job",
   "status": "available",
   "surface": "hosted",
   "safety": "Refuses, before any request: a draft without a lock (run `ks cruise approve`), a workspace over 50 MiB (.git and .keepstate excluded), a test file changed since approve (the tests_digest differs), a draft changed since approve, and a workspace changed since approve.",
   "safetyMeasured": "2026-09-16, cruise_test.go (TestCruiseRunRefusals) against a recording control plane: each refusal exits non-zero with zero requests."
  },
  {
   "verb": "cruise status",
   "aliases": [],
   "usage": "ks cruise status [job]",
   "summary": "the job (or the newest): state, rung, each attempt, save points, spend, verdict, artifact",
   "status": "available",
   "surface": "hosted",
   "note": "absent values read \"unavailable\", never zero"
  },
  {
   "verb": "cruise logs",
   "aliases": [],
   "usage": "ks cruise logs <job>",
   "summary": "the event stream, one event per line: seq, ts, type, detail",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "cruise cancel",
   "aliases": [],
   "usage": "ks cruise cancel <job>",
   "summary": "cancel a job",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "cruise resume",
   "aliases": [],
   "usage": "ks cruise resume <job> [--ladder family:model,...]",
   "summary": "resume a job from review, on the same or a wider ladder",
   "status": "available",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--ladder family:model,...",
     "summary": "the rungs to continue on, in order"
    }
   ]
  },
  {
   "verb": "cruise artifact",
   "aliases": [],
   "usage": "ks cruise artifact <job> [--out FILE]",
   "summary": "download the accepted tarball",
   "status": "available",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--out FILE",
     "summary": "where to write it; default JOB.tar.gz"
    }
   ],
   "safety": "Verifies the download's sha256 against the job's artifact_sha before the named file exists; a mismatch is refused and nothing is written.",
   "safetyMeasured": "2026-09-16, cruise_test.go (TestCruiseArtifactVerifiesBeforeWriting) against a control plane serving a tampered artifact."
  },
  {
   "verb": "cruise models",
   "aliases": [],
   "usage": "ks cruise models",
   "summary": "the model table the control plane serves: families, rungs, default ladder",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "doctor",
   "aliases": [],
   "usage": "ks doctor",
   "summary": "connectivity, token, version",
   "status": "available",
   "surface": "client"
  },
  {
   "verb": "update",
   "aliases": [],
   "usage": "ks update",
   "summary": "self-update (checksum-verified)",
   "status": "available",
   "surface": "client"
  },
  {
   "verb": "logout",
   "aliases": [],
   "usage": "ks logout",
   "summary": "remove the stored token",
   "status": "available",
   "surface": "client"
  },
  {
   "verb": "uninstall",
   "aliases": [],
   "usage": "ks uninstall",
   "summary": "how to remove ks completely",
   "status": "available",
   "surface": "client"
  },
  {
   "verb": "version",
   "aliases": [],
   "usage": "ks version",
   "summary": "print the version",
   "status": "available",
   "surface": "client"
  },
  {
   "verb": "operation show",
   "aliases": [],
   "usage": "ks operation show <operation>",
   "summary": "read an operation back by its id or key: state, result, timestamps",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "operation wait",
   "aliases": [],
   "usage": "ks operation wait <operation>",
   "summary": "wait, within the bound, for an operation to finish and print its result",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "completion",
   "aliases": [],
   "usage": "ks completion <shell>",
   "summary": "print a shell completion script generated from the command schema",
   "status": "available",
   "surface": "client"
  },
  {
   "verb": "reference",
   "aliases": [],
   "usage": "ks reference",
   "summary": "print the command reference as a Markdown table generated from the command schema",
   "status": "available",
   "surface": "client"
  },
  {
   "verb": "session list",
   "aliases": [
    "ls"
   ],
   "usage": "ks session list [--state STATE] [--all]",
   "summary": "your sessions, newest activity first: state, agent, task, last activity, last save",
   "status": "available",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--state STATE",
     "summary": "only running, parked or dead"
    },
    {
     "flag": "--all",
     "summary": "include dead sessions"
    }
   ]
  },
  {
   "verb": "session show",
   "aliases": [],
   "usage": "ks session show <session>",
   "summary": "one session in full; a short id is accepted when it is unique among yours",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "cruise preview",
   "aliases": [
    "workspace preview"
   ],
   "usage": "ks cruise preview [--allow PATH...]",
   "summary": "what an upload would carry and what it would not, with reasons: names and sizes, never content",
   "status": "available",
   "surface": "client",
   "flags": [
    {
     "flag": "--allow PATH...",
     "summary": "review a sensitive or ignored path into the upload (repeatable); never a symlink or .git"
    }
   ]
  },
  {
   "verb": "session key set",
   "aliases": [],
   "usage": "ks session key set <session> [--key KEY]",
   "summary": "bind a session's provider to one of your keys by id; that key is used or nothing is",
   "status": "available",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--key KEY",
     "summary": "the key's id or alias"
    }
   ]
  },
  {
   "verb": "key list",
   "aliases": [],
   "usage": "ks key list",
   "summary": "your keys: id, provider, alias, last four characters, enabled or disabled",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "key show",
   "aliases": [],
   "usage": "ks key show <key>",
   "summary": "one key's metadata",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "key add",
   "aliases": [],
   "usage": "ks key add [--provider PROVIDER] [--alias NAME]",
   "summary": "store a provider key read from standard input; it is never an argument and never comes back",
   "status": "available",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--provider PROVIDER",
     "summary": "anthropic, openai or openrouter"
    },
    {
     "flag": "--alias NAME",
     "summary": "a name for the key"
    }
   ]
  },
  {
   "verb": "key enable",
   "aliases": [],
   "usage": "ks key enable <key>",
   "summary": "allow new admissions with a key",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "key disable",
   "aliases": [],
   "usage": "ks key disable <key>",
   "summary": "refuse new admissions with a key; in-flight usage is reconciled",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "key delete",
   "aliases": [],
   "usage": "ks key delete <key>",
   "summary": "plan a key deletion, then execute it with --yes",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "preflight",
   "aliases": [],
   "usage": "ks preflight [--provider PROVIDER] [--mode MODE]",
   "summary": "what a run needs and what is missing: identity, keys, credit, capabilities, and the local upload preview; starts nothing",
   "status": "available",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--provider PROVIDER",
     "summary": "the provider the run will use (default anthropic)"
    },
    {
     "flag": "--mode MODE",
     "summary": "what you are about to start: cruise (default) or agent"
    }
   ]
  },
  {
   "verb": "agent list",
   "aliases": [],
   "usage": "ks agent list [--session ID]",
   "summary": "the agents of a session: name, id, activity, role, current task",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agents live in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  },
  {
   "verb": "agent open",
   "aliases": [],
   "usage": "ks agent open <name> [--session ID] [--take-control] [--view] [--no-follow]",
   "summary": "the agent window: its state, then its events as they happen",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--take-control",
     "summary": "take the control lease from the window that holds it; without it a held agent is refused, never taken"
    },
    {
     "flag": "--view",
     "summary": "open a watching window: it follows the agent and steers nothing, so it opens beside a window that holds control and never takes it"
    },
    {
     "flag": "--no-follow",
     "summary": "print the window's current state and exit, following nothing"
    }
   ]
  },
  {
   "verb": "agent status",
   "aliases": [],
   "usage": "ks agent status <name> [--session ID]",
   "summary": "one agent's activity, queue depth and current task",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  },
  {
   "verb": "agent tell",
   "aliases": [],
   "usage": "ks agent tell <name> <instruction> [--session ID]",
   "summary": "queue one instruction for an agent from outside any window",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  },
  {
   "verb": "agent queue show",
   "aliases": [],
   "usage": "ks agent queue show <name> [--session ID]",
   "summary": "why an agent's queue is not moving: what blocks it, what waits behind it, what each choice would do",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  },
  {
   "verb": "agent queue resume",
   "aliases": [],
   "usage": "ks agent queue resume <name> [--session ID] [--finding TEXT]",
   "summary": "continue the instructions held behind a failed or unresolved one; that one is never restarted",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--finding TEXT",
     "summary": "what you established about the blocked instruction; required, and recorded with the decision"
    }
   ]
  },
  {
   "verb": "agent queue hold",
   "aliases": [],
   "usage": "ks agent queue hold <name> [--session ID] [--finding TEXT]",
   "summary": "record that an agent's queue stays held, and what you established",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--finding TEXT",
     "summary": "what you established about the blocked instruction; required, and recorded with the decision"
    }
   ]
  },
  {
   "verb": "task resume",
   "aliases": [],
   "usage": "ks task resume <instruction> [--session ID] [--checkpoint ID] [--finding TEXT] [--accept-affected DIGEST]",
   "summary": "run one blocked instruction again as a new attempt from a saved point you name; it never continues the rest of the queue instead",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available. Measured: with no --checkpoint it lists the session's restorable saved points and refuses, sending nothing; with one it restores from THAT saved point only, never substituting another, and it releases no held instruction on any path",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the instruction belongs to; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--checkpoint ID",
     "summary": "the saved point to start the new attempt from; required, and never chosen for you"
    },
    {
     "flag": "--finding TEXT",
     "summary": "why you are running it again; required, and recorded with the restore"
    },
    {
     "flag": "--accept-affected DIGEST",
     "summary": "the digest the service printed with the exact list of other work a restore would return to that moment; consent is bound to that list and never to the word yes"
    }
   ]
  },
  {
   "verb": "agent approve",
   "aliases": [
    "approval approve"
   ],
   "usage": "ks agent approve <request> [--session ID]",
   "summary": "approve one permission request an agent is waiting on, by its id",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the request belongs to; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  },
  {
   "verb": "agent deny",
   "aliases": [
    "approval deny"
   ],
   "usage": "ks agent deny <request> [--session ID]",
   "summary": "deny one permission request an agent is waiting on, by its id",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the request belongs to; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  },
  {
   "verb": "agent pause",
   "aliases": [],
   "usage": "ks agent pause <name> [--session ID]",
   "summary": "save the session an agent lives in and park it, waiting until it is parked",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  },
  {
   "verb": "agent resume",
   "aliases": [],
   "usage": "ks agent resume <name> [--session ID]",
   "summary": "resume the parked session an agent lives in, waiting out a save that is still completing",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  },
  {
   "verb": "task list",
   "aliases": [
    "task ls"
   ],
   "usage": "ks task list [--session ID] [--agent NAME]",
   "summary": "the instructions of a session's agents, in the order they were committed",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session whose instructions to read; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--agent NAME",
     "summary": "read one agent's queue instead of every agent's"
    }
   ]
  },
  {
   "verb": "task show",
   "aliases": [],
   "usage": "ks task show <instruction> [--session ID]",
   "summary": "one instruction in full: where it sits, what it says, what it last reported and what attempt exists",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "refuse unless the instruction belongs to this session; optional, and only ever a guard"
    }
   ]
  },
  {
   "verb": "session checkpoints",
   "aliases": [],
   "usage": "ks session checkpoints <session>",
   "summary": "the saved points a restore may be named against: boundary, state, scope and manifest identity",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not"
  },
  {
   "verb": "task cancel",
   "aliases": [],
   "usage": "ks task cancel <instruction> [--session ID] [--reason TEXT]",
   "summary": "end one instruction: it will not run, and the instructions held behind it stay held",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the instruction belongs to; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--reason TEXT",
     "summary": "why you are cancelling it; optional, and recorded with the request"
    }
   ]
  },
  {
   "verb": "task reconcile",
   "aliases": [],
   "usage": "ks task reconcile <instruction> [--session ID] [--finding TEXT]",
   "summary": "record that a running attempt's outcome could NOT be established, after the service refused its close",
   "status": "planned",
   "surface": "hosted",
   "note": "the binary dispatches this verb; it runs only where the control plane reports the agent.workspace capability available, and refuses with that reason where it does not",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the instruction belongs to; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--finding TEXT",
     "summary": "what you established about the attempt; required, and recorded with the decision"
    }
   ]
  },
  {
   "verb": "adviser connect",
   "aliases": [],
   "usage": "ks adviser connect <adviser> [--session ID] [--agent NAME] [--confirm SESSION/AGENT] [--expires DURATION] [--max-consultations N] [--max-tokens N] [--max-output-tokens N]",
   "summary": "let an agent consult another agent of yours, after you have seen both ends and confirmed the adviser by name",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session of the agent that will ask; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--agent NAME",
     "summary": "the agent that will ask (default main)"
    },
    {
     "flag": "--confirm SESSION/AGENT",
     "summary": "confirm the adviser without a prompt; it must name the adviser shown, as session/agent or by its id"
    },
    {
     "flag": "--expires DURATION",
     "summary": "how long the connection lasts (default 24h, at most 720h)"
    },
    {
     "flag": "--max-consultations N",
     "summary": "how many consultations the connection carries (default 50)"
    },
    {
     "flag": "--max-tokens N",
     "summary": "input plus output tokens admitted per consultation (default 16384)"
    },
    {
     "flag": "--max-output-tokens N",
     "summary": "output tokens admitted per consultation (default 4096)"
    }
   ]
  },
  {
   "verb": "adviser list",
   "aliases": [],
   "usage": "ks adviser list [--session ID] [--agent NAME]",
   "summary": "an agent's adviser connections, live, revoked and expired, in both directions",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--agent NAME",
     "summary": "the agent (default main)"
    }
   ]
  },
  {
   "verb": "adviser disconnect",
   "aliases": [],
   "usage": "ks adviser disconnect <grant>",
   "summary": "withdraw a connection: every future consultation over it stops at once",
   "status": "planned",
   "surface": "hosted"
  },
  {
   "verb": "session use",
   "aliases": [],
   "usage": "ks session use [session] [--clear] [--trust-repo] [--confirm SESSION]",
   "summary": "bind this project to a session, so commands without --session mean it: per account, project root and control plane",
   "status": "available",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--clear",
     "summary": "remove this project's binding"
    },
    {
     "flag": "--trust-repo",
     "summary": "adopt the session this repository's .keepstate/session.json names, once you confirm it with --confirm"
    },
    {
     "flag": "--confirm SESSION",
     "summary": "the id of the session the repository names; required with --trust-repo, never implied by --yes"
    }
   ]
  },
  {
   "verb": "session rename",
   "aliases": [],
   "usage": "ks session rename <session> <new-name>",
   "summary": "rename a session by id; its id and everything in it are unchanged",
   "status": "available",
   "surface": "hosted"
  },
  {
   "verb": "agent rename",
   "aliases": [],
   "usage": "ks agent rename <name> <new-name> [--session ID]",
   "summary": "rename an agent by id; its tasks, approvals and adviser connections keep resolving",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  },
  {
   "verb": "result list",
   "aliases": [],
   "usage": "ks result list [--task ID] [--session ID] [--agent NAME]",
   "summary": "the results of one task, or of a session's tasks: name, size, sha256, task, verification",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--task ID",
     "summary": "one instruction's results"
    },
    {
     "flag": "--session ID",
     "summary": "every task of this session's agents; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--agent NAME",
     "summary": "only this agent's tasks (with --session)"
    }
   ]
  },
  {
   "verb": "result show",
   "aliases": [],
   "usage": "ks result show <result>",
   "summary": "one result: size, sha256, task and attempt, the producer's checks, the task's verification, provenance",
   "status": "planned",
   "surface": "hosted"
  },
  {
   "verb": "result download",
   "aliases": [],
   "usage": "ks result download <result> [--out FILE] [--force] [--extract DIR] [--task ID] [--grant-ttl DURATION]",
   "summary": "download a result, verify its size and sha256, and place it without replacing anything",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--out FILE",
     "summary": "where to place it; default the result's own file name in this directory"
    },
    {
     "flag": "--force",
     "summary": "replace an existing file at the target (atomically, and only with verified bytes)"
    },
    {
     "flag": "--extract DIR",
     "summary": "unpack a tar, tar.gz or zip result into this NEW directory after every entry passes the C09 checks"
    },
    {
     "flag": "--task ID",
     "summary": "refuse unless the result belongs to this task"
    },
    {
     "flag": "--grant-ttl DURATION",
     "summary": "how long the download grant lives (1s to 1h; default the service's 10m)"
    }
   ]
  },
  {
   "verb": "agent logs",
   "aliases": [],
   "usage": "ks agent logs [--session ID] [--follow] [--cursor CURSOR] [--limit N]",
   "summary": "the session's setup log and its agent's log from a cursor, safe to print; --follow polls until the service says it ends",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--follow",
     "summary": "keep reading new lines from the cursor until the service says following ends (a session that pauses ends it) or Ctrl-C"
    },
    {
     "flag": "--cursor CURSOR",
     "summary": "start after this cursor (from a previous page)"
    },
    {
     "flag": "--limit N",
     "summary": "at most N lines per page"
    }
   ]
  },
  {
   "verb": "file list",
   "aliases": [],
   "usage": "ks file list [path] [--session ID]",
   "summary": "one directory of a session's workspace: type, mode, size, name; secret files marked redacted",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session whose workspace to read; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  },
  {
   "verb": "file show",
   "aliases": [],
   "usage": "ks file show <path> [--session ID] [--max-bytes N] [--out FILE]",
   "summary": "one workspace file, at most 1 MiB, printed as safe text or written to a new file",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session whose workspace to read; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--max-bytes N",
     "summary": "read at most N bytes (default and maximum 1 MiB)"
    },
    {
     "flag": "--out FILE",
     "summary": "write the bytes to this NEW file instead of printing them"
    }
   ]
  },
  {
   "verb": "agent queue list",
   "aliases": [],
   "usage": "ks agent queue list <name> [--session ID]",
   "summary": "an agent's pending work in dispatch order, the instruction executing now apart, and the queue revision a move names",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  },
  {
   "verb": "task move",
   "aliases": [],
   "usage": "ks task move <instruction> [--session ID] [--before TASK] [--after TASK] [--queue-revision QREV]",
   "summary": "move one queued instruction before or after another pending one of the same agent, against a queue revision",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the agent lives in; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--before TASK",
     "summary": "place it just before this pending instruction"
    },
    {
     "flag": "--after TASK",
     "summary": "place it just after this pending instruction"
    },
    {
     "flag": "--queue-revision QREV",
     "summary": "the queue revision of the view you decided from (ks agent queue list prints it); without it the queue is read now and shown first"
    }
   ]
  },
  {
   "verb": "approval list",
   "aliases": [],
   "usage": "ks approval list [--session ID] [--all]",
   "summary": "a session's permission requests with the instruction each was asked for, what it touches and what deciding costs",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the request belongs to; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    },
    {
     "flag": "--all",
     "summary": "every request, decided ones too, with who decided and when"
    }
   ]
  },
  {
   "verb": "approval show",
   "aliases": [],
   "usage": "ks approval show <request> [--session ID]",
   "summary": "one permission request in full: the action, its instruction, what it touches, the cost, expiry and decision",
   "status": "planned",
   "surface": "hosted",
   "flags": [
    {
     "flag": "--session ID",
     "summary": "the session the request belongs to; a short id is accepted when it is unique among yours; without it, this project's binding (ks session use)"
    }
   ]
  }
 ]
}
